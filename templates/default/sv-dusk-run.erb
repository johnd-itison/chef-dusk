#!/bin/sh

if [ -f /opt/rbenv/shims/ruby ]; then
  export PATH=/opt/rbenv/shims:$PATH
fi

env=$PWD/env

exec 2>&1

cd <%= @options[:root] %>/current
exec chpst \
     -e $env \
     -u <%= @options[:user] %> \
     ./bin/thin start
